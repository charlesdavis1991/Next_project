import React from "react";

const CheckSaveButton = ({}) => {
  return (
    <div
      className="col pl-0 p-r-5"
      style={{
        width: "194px",
        justifyItems: "end",
      }}
      //   onClick={(e) => {
      //     e.stopPropagation();
      //     onClick(null);
      //   }}
    >
      {/* <button
        style={{
          background: "#6c757d",
          borderColor: "#6c757d",
          color: "white",
          fontSize: "14px",
        }}
        className="height-25 inbox-save-btn btn delete-document green-bg-hover bg-secondary d-flex align-items-center justify-content-center"
      >
        Save
      </button> */}
    </div>
  );
};

export default CheckSaveButton;
